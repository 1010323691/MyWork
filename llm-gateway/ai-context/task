AI 网关转发与计费系统需求文档 (v1.1)
1. 项目概述
本项目旨在构建一个 AI 网关代理层，实现对大模型 API 的统一转发、鉴权、流量控制及计费管理。网关需兼容 OpenAI 协议标准，支持管理员配置上游算力资源，并为最终用户提供稳定的 API 调用服务。

2. 用户角色
系统管理员：拥有后台管理权限，负责配置上游模型、查看日志、监控系统状态。
终端用户：持有 API Key，通过网关调用大模型服务，需消耗相应额度。
3. 功能需求
3.1 管理端功能
3.1.1 导航栏配置
在现有的管理员后台导航栏中，新增 “网关转发管理” 主菜单入口。
该菜单下应包含 “模型资源管理” 与 “计费规则配置” 子模块。
3.1.2 上游模型资源管理 (CRUD)
功能描述：管理员维护网关转发的上游算力通道配置。
操作内容：支持对以下字段进行增、删、改、查操作：
模型名称 (Model Name)：内部唯一标识（如 gpt-3.5-turbo）。
上游服务地址 (Upstream URL)：厂商提供的 API 接口地址。
上游密钥 (Upstream API Key)：用于访问上游厂商的凭证。
模型状态：启用/禁用（用于快速切断不可用通道）。
数据安全：
加密存储：数据库存储的“上游密钥”必须进行加密处理（推荐使用 AES-256 算法），禁止明文展示。前端编辑时仅显示掩码（如 sk-****1234）。
3.1.3 计费规则配置
功能描述：支持动态调整计费策略，避免硬编码。
配置项：
输入 Token 折算率：默认值为 0.2 (即 1/5)，支持管理员全局或按模型单独配置。
输出 Token 折算率：默认值为 1.0，支持调整。
计费单位：明确 Token 与金额或积分的换算关系。
3.2 网关核心转发逻辑
3.2.1 接口协议标准
交互方式：完全兼容 OpenAI 标准协议。
请求参数：用户请求需携带 url、model、user_api_key。
鉴权验证：
网关解析请求头中的 API Key。
查询 用户密钥表，验证 Key 是否有效、是否过期、是否被禁用。
安全增强：建议增加 IP 白名单校验功能，防止 Key 被盗用。
3.2.2 额度校验与路由
余额预检：在转发前，校验该用户 Key 绑定的账户是否有足够的 Token 余额（需考虑并发，防止超扣）。
路由查找：根据用户请求的 model 名称，从上游模型配置表中匹配对应的上游 URL 和上游 Key。
请求转发：将用户请求中的敏感信息替换为上游 Key，转发至算力中心。
3.2.3 异常处理与稳定性
超时控制：设置上游请求超时时间（如 60s），超时则主动断开并返回错误。
重试机制：对于上游返回的临时性错误（如 5xx），支持自动重试（配置重试次数，如 2 次）。
熔断降级：当某上游通道连续失败达到阈值，自动将其状态标记为“熔断”，暂时停止转发，避免拖垮网关。
流式响应 (Stream) 支持：
若用户开启 stream: true，网关需支持透传流式数据（SSE）。
计费优化：流式模式下，Token 统计可能分多次到达。建议采用 “预估扣减 + 最终对账” 模式，或异步统计总 Token 后扣费，避免阻塞用户响应。
3.3 计费与日志系统
3.3.1 日志记录 (Log)
记录内容：每次请求需记录时间、用户 ID、模型名称、输入 Token 数、输出 Token 数、状态码、上游响应耗时。
数据存储：
写入专用日志表 (log_request 或 bill_record)。
性能优化：日志表数据量大，需设计分库分表策略（如按月分表），并定期归档冷数据，保证主表查询性能。
索引优化：对 user_id、request_time、status 建立索引，便于后续账单查询与审计。
3.3.2 额度扣减逻辑
计算公式：
消耗额度 = (输入 Token 数 × 输入折算率) + (输出 Token 数 × 输出折算率)
注：默认输入折算率为 1/5，但需支持后端动态读取配置。
并发控制：
扣减操作必须保证原子性。
使用 数据库乐观锁 (Version 字段) 或 Redis 分布式锁 防止高并发下的余额超扣（负数）问题。
异步扣减：对于流式接口，建议先记录临时日志，待请求完全结束后再统一计算并扣减额度，确保准确性。
3.3.3 用户余额查询
提供独立的 API 接口（如 GET /v1/balance），用户可通过此接口查询当前剩余 Token 额度。
在网关返回的标准响应 Header 中，建议增加 X-Remaining-Balance 字段，提示用户剩余量。
3.4 错误提示优化
鉴权失败：返回 401，提示“无效的 API Key"。
额度不足：返回 402 (Payment Required) 或 403，明确提示“账户余额不足，请充值”。
上游服务异常：返回 503，提示“服务繁忙，请稍后重试”，避免暴露上游内部错误详情。
4. 非功能需求
4.1 安全性 (Security)
密钥管理：所有 API Key（用户 Key 与上游 Key）在数据库中必须加密存储，严禁明文。
传输加密：全站强制 HTTPS。
日志脱敏：日志中严禁记录用户的真实 Prompt 敏感内容（可选），如需记录需进行脱敏处理。
4.2 性能 (Performance)
并发支持：网关需支持至少 QPS 1000+ 的高并发请求。
响应延迟：除网络耗时外，网关处理（鉴权、计费）延迟应控制在 100ms 以内。
4.3 可扩展性 (Scalability)
模型配置化：系统架构应支持随时新增上游厂商（如 Azure, Anthropic, 国内厂商等），无需修改核心代码，仅通过配置页添加即可。
计费策略灵活：预留计费插件接口，未来可支持“按次计费”、“包月套餐”等多种模式，而非仅支持 Token 扣除。
5. 数据模型简述 (参考)
User_Key_Table (用户密钥表)
id, user_id, api_key (加密), balance (余额), status (状态), created_at, version (用于乐观锁)。
Upstream_Provider_Table (上游提供商表)
id, model_name, upstream_url, upstream_key (加密), input_rate (默认 0.2), output_rate (默认 1.0), status。
Request_Log_Table (请求日志表)
id, request_id, user_key_id, model_name, input_tokens, output_tokens, total_cost, status, response_time, created_at。