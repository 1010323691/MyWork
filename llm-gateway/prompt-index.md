你是高级软件工程师。请对项目进行结构化分析，并生成 ai-context 文档（用于后续精确修改）。

【工程路径】
D:\git\GitHub\MyWork\llm-gateway

【目标】
支持后续修改时：
- 快速定位文件
- 判断影响范围
- 理解调用链与数据流

【强约束】
P0：严禁输出代码（不允许方法体/类实现/大段代码，仅允许 文件名/类名/方法名）
P1：只做“修改导向分析”，禁止泛讲
P2：关键路径必须包含 文件名+方法名
P3：不确定就说明需读取文件，禁止猜测

【分析范围】
仅核心路径：入口 / 主调用链 / 核心数据流 / 模块边界（忽略边缘模块）

【输出内容】

1. 入口与结构  
- 后端入口（main）  
- 前端入口（index / main / App）  
- 核心目录+关键文件（简要说明）

2. 模块划分（全栈）  
后端：controller / service / repository / dto / config / security等  
前端：ui / api  
每个模块：关键文件 + 职责 + 依赖关系

3. 前后端接口映射  
前端 API → 后端 controller（文件+方法）

4. 关键调用链  
用户操作 → 前端 → API → 后端 → 返回  
每一步必须：文件名 + 方法名

5. 数据流  
前端数据 → API参数 → 后端接收 → 校验/处理 → 持久化/返回 → 前端更新

6. 修改定位指南  
问题类型 → 文件(+方法) → 原因  
必须包含：  
UI异常 / 事件无效 / API错误 / 接口异常 / 鉴权问题 / 路由转发 / 数据库问题 / 配置问题 / 日志计费

7. 修改影响面  
核心文件变更 → 影响模块/接口 + 原因

8. 风险点  
强耦合 / 易错逻辑 / 修改需联动的文件

【输出结构】

/ai-context/
  index.md（仅导航，禁止细节）
  frontend/{ui,api}.md
  backend/{controller,service,repository,dto,config}.md
  api-mapping.md
  risk.md

【index.md限制】
仅允许：路径 + 模块划分 + 跳转指引 + 阅读顺序  
禁止：实现细节

【补充】
项目过大只分析核心路径；信息不足必须指出需补读文件。

开始分析。