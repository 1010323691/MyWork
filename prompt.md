你是一个高级软件工程师，请对当前项目进行“结构化深度分析”，用于后续精确修改代码。

path：D:\projectPath\MyWork\多并发测试

【目标】
让后续任何修改请求，都可以基于你的总结：
- 快速定位应该修改的文件
- 明确影响范围（关联文件）
- 理解调用链和数据流
- 避免误改无关代码

======================

【分析要求】

请严格按以下结构输出：

一、项目结构总览
- 列出所有关键文件/模块
- 每个文件一句话说明作用
- 标出入口文件（如 index / main / app）

二、核心模块划分（非常重要）
将项目拆分为逻辑模块，例如：
- UI层
- 状态管理
- 工具函数
- API通信
- 业务逻辑

每个模块说明：
- 包含哪些文件
- 模块职责
- 与其他模块的依赖关系

三、关键调用链（核心）
列出“从用户操作到结果”的完整流程，例如：

用户点击按钮 →
调用哪个函数 →
经过哪些文件 →
如何处理数据 →
最终更新哪里

要求：
- 用步骤编号
- 标明文件名 + 函数名

四、关键数据流
说明项目中重要数据是如何流动的，例如：
- 状态从哪里来
- 如何传递
- 在哪里被修改

五、修改定位指南（最关键）
请总结出：

当遇到以下类型问题时，应该优先查看哪些文件：

- UI不生效 →
- 点击事件无效 →
- 数据未更新 →
- API异常 →
- 样式问题 →

格式如下：

问题类型 → 相关文件 → 原因说明

六、文件关联关系（依赖图）
列出：

文件A 修改时，可能影响：
- 文件B（原因）
- 文件C（原因）

七、潜在风险点（重要）
指出：
- 容易出bug的地方
- 强耦合区域
- 修改时需要特别小心的逻辑

======================

【输出要求】

- 不要贴完整代码
- 必须使用结构化表达（分点、分模块）
- 必须包含“文件名 + 函数名”
- 描述要具体，不要泛泛而谈
- 优先保证“可用于修改”，而不是泛解释
- 将分析结果输出到工程根目录下ai-context文件夹内(若没有则创建)
/ai-context/
  index.md          ← 全局索引
    ※ 本文件必须保持极度精简，仅用于说明工程根路径（如：/project-root/xxx）及各子模块的职责与跳转指引，禁止编写详细实现内容。
    ※ 执行任务前必须先阅读本索引，根据需求选择对应模块（ui/state/logic/api/risk）查阅；每轮任务完成后，需汇总本轮修改点并执行 git commit，提交说明必须包含：修改模块 + 具体变更内容 + 修改目的。
  ui.md             ← UI模块
  state.md          ← 状态管理
  logic.md          ← 核心业务逻辑
  api.md            ← 接口相关
  risk.md           ← 风险点（可选）

======================

【补充规则】

如果信息不足：
- 明确指出需要读取哪些文件
- 不要猜测

如果项目较大：
- 优先分析核心路径
- 可忽略边缘模块

======================

请开始分析。